{
  "name": "@archsmith/schema",
  "version": "0.5.0",
  "description": "IR schema and governed registries (sub-layers, colors, icons) for ArchSmith diagrams",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "diagram-schema.json",
    "registries"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "cd dist && node --test"
  }
}
