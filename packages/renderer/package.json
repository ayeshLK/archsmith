{
  "name": "@archsmith/renderer",
  "version": "0.1.0",
  "description": "ArchSmith diagram renderer — validated IR in, SVG out. Pure library, no agent/LLM dependency.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@archsmith/schema": "^0.1.0",
    "ajv": "^8.17.1",
    "@fontsource/arimo": "^5.1.0",
    "fontkit": "^2.0.4"
  },
  "devDependencies": {
    "@types/fontkit": "^2.0.8"
  }
}
