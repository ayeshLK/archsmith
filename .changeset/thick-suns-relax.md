---
"@archsmith/cli": patch
---

Changed `archsmith author`'s text-input prompt starter from `?` to `>` (issue #94) — the more familiar convention, consistent across every screen's `TextInput` prompt.
