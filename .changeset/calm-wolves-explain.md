---
"@archsmith/schema": minor
"@archsmith/renderer": minor
"@archsmith/cli": minor
---

Allow diagrams to omit the Legend block and let `archsmith author` persist an Include/Omit Legend choice. Schema 0.3.4 makes `legend` optional, and the renderer removes both the block and its unused footer space when absent.
