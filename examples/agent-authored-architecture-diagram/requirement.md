# Original requirement

I need an ArchSmith architecture diagram for a fictional document-processing platform. Employees upload documents through a web app; the backend scans each upload for malware, extracts metadata, stores the original document and metadata, and sends a status notification.

Before drafting any IR, inspect the live ArchSmith schema and governed registries through the published MCP server. Ask concise clarifying questions for anything the schema requires or the architecture leaves ambiguous. Do not validate or render until those questions have been answered.
